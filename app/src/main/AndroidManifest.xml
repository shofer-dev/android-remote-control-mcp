<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Screen streaming. Android 14 requires the mediaProjection FGS TYPE and this matching
         permission before a capture may be obtained: MediaProjection.start throws SecurityException
         unless a foreground service of that type is already running, and the OS stops the grant the
         moment one stops. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Boot receiver -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- App Management -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <!-- Camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Location (background-location is geofence-only → declared in the gms flavor manifest) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- WiFi (Event Channel) -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />

    <!-- Foreground service type for Event Channel (location for geofence/WiFi) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Tethered-SIM MSISDN provenance (android_get_sim_info via SubscriptionManager). TWO runtime
         permissions, and both matter:
           READ_PHONE_NUMBERS — reads the number itself (SubscriptionManager.getPhoneNumber).
           READ_PHONE_STATE   — ENUMERATES the active subscriptions (getActiveSubscriptionInfoList),
                                which the multi-SIM "return all active subs" requirement needs.
         BOTH are protectionLevel=dangerous and grantable via `pm grant` on a RETAIL (user) build,
         exactly like the accessibility/DUMP grants the provisioning path already issues. Neither is
         the privileged READ_PRIVILEGED_PHONE_STATE — that one the adb shell cannot grant on a user
         build, which is why the adb `content query .../siminfo` rung fails and this app verb exists. -->
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Media read access (optional — enables "all files" mode for built-in storage locations) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!--
      Package visibility (API 30+) for the ONE vendor screen the keep-alive hint links to:
      MIUI/HyperOS's Autostart manager. Without this entry the component is invisible to us and
      the launch fails as "not found" even on a phone that has it. Nothing else is queried — the
      hint's other two destinations are platform Settings actions, which need no declaration.
    -->
    <queries>
        <package android:name="com.miui.securitycenter" />
    </queries>

    <application
        android:name=".McpApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidRemoteControlMcp"
        tools:targetApi="34">

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AndroidRemoteControlMcp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Share Target (receives content shared INTO the app via the Android share sheet) -->
        <activity
            android:name=".ui.ShareReceiverActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:label="@string/share_target_label"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!-- MCP Server Foreground Service -->
        <service
            android:name=".services.mcp.McpServerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="MCP server providing remote device control" />
        </service>

        <!-- Platform Connector Foreground Service (outbound /ws/device client) -->
        <service
            android:name=".services.connector.PlatformConnectorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Maintains the outbound platform control connection" />
        </service>

        <!-- Screen-stream capture service (docs/phones/android_screen_streaming.md step 4).
             A SECOND foreground service beside the connector's, because a service may carry only
             one type and the connector's is specialUse — and because folding capture into it would
             tie the screen grant to the control connection's lifetime, costing a consent dialog on
             every reconnect. -->
        <service
            android:name=".services.screenstream.ScreenStreamService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- The OS capture-consent trampoline. Transparent and finishing at once: what the holder
             sees is the system's own dialog, never a screen of ours in front of it. -->
        <activity
            android:name=".services.screenstream.ScreenStreamConsentActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Enrolment Terms Activity (mid-handshake consent) -->
        <activity
            android:name=".ui.ConnectorTermsActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.AndroidRemoteControlMcp" />

        <!-- Accessibility Service -->
        <service
            android:name=".services.accessibility.McpAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Notification Listener Service -->
        <service
            android:name=".services.notifications.McpNotificationListenerService"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Boot Completed Receiver -->
        <receiver
            android:name=".services.mcp.BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- ADB Configuration Receiver (headless setup via adb shell am broadcast) -->
        <!-- Gated on DUMP: a signature/privileged platform permission held by the
             adb shell UID (com.android.shell) but NOT grantable to third-party apps.
             Enforced by ActivityManager against the sender's real binder UID, so
             `adb shell am broadcast` still works while ordinary apps get a SecurityException. -->
        <receiver
            android:name=".services.mcp.AdbConfigReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
        </receiver>

        <!-- Device Administration Receiver (the lock/wipe device-action plane, §6.4).
             Activated at enrolment; policies declared in res/xml/device_admin_policies.xml.
             BIND_DEVICE_ADMIN restricts binding to the OS. -->
        <receiver
            android:name=".services.deviceadmin.PlatformDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Event Channel Service -->
        <service
            android:name=".services.channel.EventChannelService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- ADB Service Trampoline (headless start/stop via adb shell am start) -->
        <!-- Gated on DUMP (adb-shell-only) for the same reason as AdbConfigReceiver above. -->
        <activity
            android:name=".services.mcp.AdbServiceTrampolineActivity"
            android:exported="true"
            android:permission="android.permission.DUMP"
            android:theme="@android:style/Theme.NoDisplay"
            android:excludeFromRecents="true"
            android:noHistory="true" />

    </application>

</manifest>
